# Web MTProto Client (Scaffold)

This is a placeholder wrapper for a Telegram MTProto JS client (e.g., GramJS). It will be wired into the web UI once we add the build system.

Planned steps:
- Install GramJS via npm.
- Implement `MtprotoClient` methods.
- Add login screen (phone + code) and bind to chat list.
